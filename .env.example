# Copy this file to .env and set your real domain once the site is deployed.
# It's the single source of truth for canonical URLs, Open Graph tags,
# robots.txt, and sitemap.xml (see scripts/generate-seo-files.js).
VITE_SITE_URL=https://rohitpuri.dev
